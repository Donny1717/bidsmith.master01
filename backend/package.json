{
  "name": "bidsmith-backend",
  "version": "1.0.0",
  "description": "BidSmith ASF Backend API",
  "main": "src/api/server.js",
  "scripts": {
    "dev": "nodemon src/api/server.js",
    "start": "node src/api/server.js",
    "health": "node -e \"require('node:http').get('http://localhost:3000/api/health',res=>{const ok=res.statusCode===200;res.on('data',()=>{});res.on('end',()=>process.exit(ok?0:1));}).on('error',()=>process.exit(1));\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1",
    "stripe": "^14.0.0",
    "firebase-admin": "^12.0.0",
    "openai": "^4.20.1",
    "pdf-parse": "^1.1.1",
    "mammoth": "^1.6.0",
    "puppeteer": "^21.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
